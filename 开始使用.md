# 开始使用

## 一句话安装

Codex：

```bash
curl -fsSL https://raw.githubusercontent.com/mozhou580-lgtm/vibe-coding-standardized-operation/main/install-online.sh | bash -s -- codex
```

Claude Code：

```bash
curl -fsSL https://raw.githubusercontent.com/mozhou580-lgtm/vibe-coding-standardized-operation/main/install-online.sh | bash -s -- claude
```

Windows PowerShell：下载仓库 ZIP，解压后运行：

```powershell
.\install.ps1 -Target Codex
```

## 一句话启动

```text
开始 PRD：我想做一个客户跟进工作台，请按推荐方案直接做一个最小可点击 Demo；只问会改变结果的问题。
```

## 接下来只说三句话

```text
接着做设计：把刚才确认的产品草图变成页面和关键状态。
生成 TDD 验收契约：告诉我现场看到什么才算完成。
按当前 PRD、Design 和 TDD 做出小 Demo；遇到冲突先停下来问我。
```

## 如果使用 Cloud / Word Buddy

上传仓库 ZIP 或 `skills/` 目录，先发送：

```text
请先读取 claude.md 和三个 SKILL.md，再按 PRD → Design → TDD 做一个小 Demo。
```
