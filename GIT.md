# Vibe Coding 规范化操作 · Git 协作

## 定位

Git 规范的目标是减少交接误解和变更冲突，不是让业务人员记住一套复杂命令。

## 何时使用

- 个人快速验证：只保留清晰的提交说明和可回退版本。
- 两人以上协作：增加分支、Review 和变更记录。
- 业务团队长期维护：再增加 Issue 模板、责任人和 CI 门禁。

## 最小协作约定

每次交付只需说明四件事：

1. 改了什么。
2. 为什么改。
3. 影响谁、影响哪个业务流程。
4. 如何验证，是否有截图或数据证据。

## 推荐流程

```text
提出业务问题
→ 建立任务卡
→ 说明完成标准
→ 小范围修改
→ 演示/验证
→ Review
→ 合并
```

## 角色翻译

- 业务负责人：确认问题、优先级和“像不像想要的结果”。
- 实现负责人：把需求做成可操作结果，并说明限制。
- Review 人：重点检查业务边界、数据风险和回归，不要求重写实现。

## 配置原则

- 默认不自动改远端仓库设置。
- 不自动覆盖已有 `.github` 配置；先列出冲突再决定。
- CI 只检查项目已有的构建和测试能力，不为了“看起来规范”强行新增。
- 不强制业务团队采用 GitHub Issue、CODEOWNERS 或多分支模式；根据协作规模启用。

## 失败模式与缓解

**失败模式：** 协作文件齐全，但没有人知道业务验收标准，Review 变成格式检查。

**缓解：** 任务卡和 PR 必须链接到业务版 PRD/验收清单，先看结果证据，再看代码形式。
